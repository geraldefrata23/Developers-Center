node_modules/
.env
*.log

# Compiled output of `npm run build` (tsc -p frontend/tsconfig.json).
# Regenerated by `npm start`/`npm run dev`/`npm run build` — never hand-edited.
# See README.md "Building".
frontend/dist/
